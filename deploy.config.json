{
  "$comment": [
    "Per-environment build-time configuration, read by scripts/deploy.mjs.",
    "",
    "NEXT_PUBLIC_SITE_URL is inlined by Next.js at build time and baked into",
    "canonical URLs, sitemap.xml, robots.txt and og:image during prerender —",
    "so it cannot be a Worker secret or var. It has to be correct when the",
    "build runs, which is what this file guarantees.",
    "",
    "These are public URLs, not secrets. Nothing sensitive belongs here.",
    "",
    "Production stays on workers.dev until DNS is pointed at the Worker; swap",
    "it to https://mohammednafia.com as part of that cutover, not before, or",
    "the canonical tags will name a host that does not resolve yet."
  ],

  "staging": {
    "siteUrl": "https://portfolio-staging.mohammadnafia1.workers.dev"
  },

  "production": {
    "siteUrl": "https://portfolio-production.mohammadnafia1.workers.dev"
  }
}
